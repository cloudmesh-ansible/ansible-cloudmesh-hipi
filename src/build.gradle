apply plugin: 'java'


jar {
  manifest {
    attributes("Main-Class": "org.hipi.examples.PixelIntensity")
  }

  dependencies{
    compile project(':core')
  }

  from {
    configurations.compile.collect { it.isDirectory() ? it : zipTree(it) }
  }
}

